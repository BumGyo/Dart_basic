void main() {
  var name = 'Mitae';
  var age = 19;
  var greeting =
      "Hello everyone, my name is $name and I'm ${age + 2} years old,";
  print(greeting);

  // Dart에서 변수를 출력하려면 달러기호를 붙이고 사용할 변수를 적어주면 된다.
  // 변수를 계산해서 보여줘야 한다면 중괄호를 사용하면 된다.
}
